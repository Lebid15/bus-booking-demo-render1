# OpenAPI Delta — G17 / E18

G17 adds the platform continuity and release-control surface:

- `GET /v1/platform/continuity`
- `POST /v1/platform/continuity/commands`
- `POST /v1/platform/continuity/recovery-exercises`
- `POST /v1/platform/releases`
- `GET/POST /v1/platform/incidents`
- `POST /v1/platform/incidents/{incident_id}/commands`
- `POST /v1/platform/load-tests`

The final generated contract contains **132 paths** and **157 operations**, with **0 validation errors and 0 warnings**.
